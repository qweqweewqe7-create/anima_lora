# Tagger caformer (dbv4) backend — moved to `anime_tools`

This document moved with the code in the curation split (Phase 1, 2026-08-30):
**https://github.com/sorryhyun/anime_tools/blob/main/docs/tagger_caformer_backend.md**
(sibling checkout: `../anime_tools/docs/tagger_caformer_backend.md`).

Implemented as `anime_tools.tagger.dbv4_backend`; the trainer's download target reads the torch-free `anime_tools.tagger.dbv4_meta`.
