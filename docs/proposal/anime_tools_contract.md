# `anime_tools` ↔ `anima_lora` contract — moved

The contract now lives with the package it governs:
**https://github.com/sorryhyun/anime_tools/blob/main/docs/contract.md**
(sibling checkout: `../anime_tools/docs/contract.md`). A change to any row is a
two-PR change by design; the trainer-side guard is `tests/test_curation_boundary.py`.
