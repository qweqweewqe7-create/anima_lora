# Tagger label-sharing heads (closed finding) — moved to `anime_tools`

Moved in the curation split (Phase 3b, 2026-08-30):
**https://github.com/sorryhyun/anime_tools/blob/main/docs/tagger_label_sharing_heads.md** (sibling checkout: `../anime_tools/docs/tagger_label_sharing_heads.md`). The gitignored `_archive/{anima_tagger_training,tagger_eval,tagger_factored_head}` history moved to that repo's `_archive/` alongside it.
